<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up | Responsive Auth</title>
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Load Boxicons for icons -->
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <style>
        /* Custom styles for the purple gradient and subtle floating effect */
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap');
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: #1a1a2e; /* Deep space blue background */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 1rem;
        }

        /* Input field custom styling to match the image */
        .input-box {
            position: relative;
            width: 100%;
            height: 50px;
            margin: 30px 0;
        }

        .input-box input {
            width: 100%;
            height: 100%;
            background: transparent;
            border: none;
            outline: none;
            border-bottom: 2px solid rgba(255, 255, 255, 0.4);
            padding-right: 35px;
            color: white;
            transition: all 0.3s ease;
        }

        .input-box input:focus,
        .input-box input:valid {
            border-bottom-color: #8c52ff;
        }

        .input-box label {
            position: absolute;
            top: 50%;
            left: 0;
            transform: translateY(-50%);
            color: rgba(255, 255, 255, 0.7);
            pointer-events: none;
            transition: all 0.3s ease;
        }

        .input-box input:focus ~ label,
        .input-box input:valid ~ label {
            top: -5px;
            font-size: 0.8rem;
            color: #8c52ff;
        }

        .input-box .bx {
            position: absolute;
            right: 0;
            top: 50%;
            transform: translateY(-50%);
            color: rgba(255, 255, 255, 0.5);
        }

    </style>
</head>

<body>
    <!-- Main Container (The Card) -->
    <div id="auth-container" class="bg-gray-800 rounded-3xl shadow-2xl p-0 overflow-hidden 
                                  w-full max-w-sm md:max-w-4xl 
                                  flex flex-col md:flex-row transition-all duration-500">

        <!-- Left Side: Sign Up Form -->
        <div class="form-box w-full md:w-1/2 p-8 md:p-12 order-2 md:order-1 flex flex-col justify-center">
            <h2 class="text-3xl font-bold text-white mb-6 text-center">Sign Up</h2>
            
            <form action="/signup" method="post" class="space-y-4">
                
                <!-- Username Input -->
                <div class="input-box">
                    <input type="text" id="name" name="username" required autocomplete="off">
                    <label for="name">Username</label>
                    <i class='bx bxs-user'></i>
                </div>

                <!-- Password Input -->
                <div class="input-box">
                    <input type="password" id="password" name="password" required>
                    <label for="password">Password</label>
                    <i class="bx bxs-lock-alt"></i>
                </div>
                
                <!-- Submit Button -->
                <button type="submit" class="w-full h-12 mt-6 rounded-xl text-white font-semibold 
                                            bg-gradient-to-r from-purple-500 to-indigo-600 
                                            hover:from-purple-600 hover:to-indigo-700 transition-all 
                                            shadow-lg shadow-purple-500/50">
                    Submit
                </button>
                
                <!-- Login Link -->
                <div class="regi-link text-center pt-4">
                    <p class="text-sm text-gray-400">Already have an account?
                        <a class="font-medium text-purple-400 hover:text-purple-300 transition-colors" href="/">Login</a>
                    </p>
                </div>
            </form>
        </div>
        
        <!-- Right Side: Info Content -->
        <div class="info-content w-full md:w-1/2 p-12 order-1 md:order-2 
                    bg-gradient-to-br from-purple-700 to-indigo-900 text-white 
                    flex flex-col justify-center items-center text-center">
            
            <i class='bx bxs-group text-6xl mb-4 opacity-80'></i>
            <h2 class="text-3xl font-extrabold mb-3">WELCOME!</h2>
            <p class="text-gray-200 text-lg">Enter your personal details to start your journey with us and access exclusive features.</p>
        </div>

    </div>
    
    <!-- Script tag for your Node.js application logic -->
    <!-- NOTE: This client-side file must exist in your 'public' folder for this to work -->
    <script src="/index.js"></script> 
</body>
</html>