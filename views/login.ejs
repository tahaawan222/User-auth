<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Load Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Load Boxicons for icons -->
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <style>
        /* CSS Variables from home.html for consistency */
        :root {
            --color-dark-bg: #101014;
            --color-card-bg: #1e1e24;
            --color-purple-primary: #8a2be2; /* Blue Violet */
            --color-neon-purple: #c471ed; /* Lighter Neon */
            --color-link-light: #d6b8f5;
        }

        /* Custom styles for the purple gradient and dynamic background */
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap');
        
        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--color-dark-bg);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 1rem;
            overflow: hidden; 
            position: relative;
        }

        /* Keyframes for the Subtle Background Animation (More movement and scaling) */
        @keyframes subtleMove {
            0% { transform: translate(0, 0) scale(1); }
            50% { transform: translate(60px, -60px) scale(1.1); }
            100% { transform: translate(0, 0) scale(1); }
        }

        /* NEW: Static, massive blurred gradient layer for deep atmosphere */
        .bg-fixed-gradient {
            position: fixed; 
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            /* Massive, highly transparent radial gradient */
            background: radial-gradient(circle at 75% 10%, var(--color-neon-purple) 0%, transparent 20%),
                        radial-gradient(circle at 25% 90%, var(--color-purple-primary) 0%, transparent 30%);
            /* Use a CSS filter to achieve the blur effect */
            filter: blur(150px); 
            z-index: 0;
            opacity: 0.3; /* Keep it subtle */
        }


        /* Animated Background Element (Now moving particles) */
        .bg-moving-dots {
            position: absolute;
            top: 0;
            left: 0;
            width: 200%;
            height: 200%;
            /* Smaller, less opaque circles for a "star" or "particle" effect */
            background: radial-gradient(circle at 15% 5%, rgba(255, 255, 255, 0.1) 1%, transparent 5%),
                        radial-gradient(circle at 85% 95%, rgba(255, 255, 255, 0.08) 1%, transparent 7%),
                        radial-gradient(circle at 40% 60%, rgba(196, 113, 237, 0.2) 1%, transparent 10%);
            animation: subtleMove 50s infinite alternate; /* Slower animation */
            z-index: 1; 
        }
        
        /* Ensure card sits above the background and gets a glass effect */
        #auth-container {
            z-index: 10; 
            position: relative;
            /* Added backdrop blur for the card itself for a subtle glass effect */
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);
        }

        /* Custom glow for the main card (matches home.html) */
        .card-glow {
            box-shadow: 0 0 40px rgba(138, 43, 226, 0.8), /* Increased intensity */
                        0 0 100px rgba(138, 43, 226, 0.3); /* Deeper glow */
            border: 1px solid rgba(138, 43, 226, 0.3); /* Subtle edge definition */
        }

        /* Input field custom styling - Unchanged */
        .input-box {
            position: relative;
            width: 100%;
            height: 50px;
        }

        .input-box input {
            width: 100%;
            height: 100%;
            background: transparent;
            border: none;
            outline: none;
            border-bottom: 2px solid rgba(255, 255, 255, 0.4);
            padding-right: 35px;
            color: white;
            transition: all 0.3s ease;
        }

        .input-box input:focus,
        .input-box input:valid {
            border-bottom-color: var(--color-neon-purple); 
        }

        .input-box label {
            position: absolute;
            top: 50%;
            left: 0;
            transform: translateY(-50%);
            color: rgba(255, 255, 255, 0.7);
            pointer-events: none;
            transition: all 0.3s ease;
        }

        .input-box input:focus ~ label,
        .input-box input:valid ~ label {
            top: -5px;
            font-size: 0.8rem;
            color: var(--color-neon-purple); 
        }
        
        .input-box .bx {
            position: absolute;
            right: 0;
            top: 50%;
            transform: translateY(-50%);
            color: rgba(255, 255, 255, 0.5);
        }

    </style>
</head>

<body>
    <!-- NEW: Static, blurred gradient element for depth -->
    <div class="bg-fixed-gradient"></div>
    <!-- UPDATED: Animated Background Element for moving particles/dots -->
    <div class="bg-moving-dots"></div>

    <!-- Main Container (Single Card Style - Matches Dashboard) -->
    <div id="auth-container" class="bg-[var(--color-card-bg)] rounded-2xl p-8 md:p-12 
                                  w-full max-w-md transition-all duration-500 card-glow bg-opacity-80">
        
        <!-- Login Icon -->
        <div class="flex justify-center mb-6">
            <i class='bx bxs-lock-open text-6xl text-purple-400 opacity-80'></i>
        </div>

        <h2 class="text-3xl font-bold text-white mb-8 text-center">Portal Login</h2>
        
        <!-- UPDATED: Using space-y-8 for responsive vertical spacing -->
        <form action="/login" method="post" class="space-y-8"> 
            
            <!-- Username Input -->
            <div class="input-box">
                <input type="text" id="name" name="username" required autocomplete="off">
                <label for="name">Username</label>
                <i class='bx bxs-user'></i>
            </div>

            <!-- Password Input -->
            <div class="input-box">
                <input type="password" id="password" name="password" required>
                <label for="password">Password</label>
                <i class="bx bxs-lock-alt"></i>
            </div>
            
            <!-- Submit Button -->
            <button type="submit" class="w-full h-12 rounded-xl text-white font-semibold 
                                        bg-gradient-to-r from-purple-500 to-indigo-600 
                                        hover:from-purple-600 hover:to-indigo-700 transition-all duration-300
                                        shadow-lg shadow-purple-500/70 hover:scale-[1.02] transform">
                Log In Securely
            </button>
            
            <!-- Signup Link -->
            <div class="regi-link text-center pt-4">
                <p class="text-sm text-gray-400">Don't have an account?
                    <a class="font-medium text-purple-400 hover:text-purple-300 transition-colors" href="/signup">Create One Now</a>
                </p>
            </div>
        </form>
    </div>
    
    <!-- Script tag for your Node.js application logic -->
    <script src="/index.js"></script> 
</body>
=======

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link rel="icon" type="image/png" href="/favicon.png">
  <link rel="stylesheet" href="/style.css">
  <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
</head>

<body>
  <div class="container">
    <div class="curved-shape"></div>
    <div class="form-box Login">
      <h2>Login</h2>
      <form action="/login" method="post">
        <div class="input-box">
          <input type="text" id="name" name="username" required autocomplete="off">
          <label for="">Username</label>
          <i class='bx bxs-user'></i>
        </div>

        <div class="input-box">
          <input type="password" id="password" name="password"  required>
          <label for="">password</label>
          <i class="bx bxs-lock-alt"></i>
        </div>
        <div class="input-box">
          <button type="submit" class="btn">Login</button>
        </div>
        <div class="regi-link">
          <p>Don't have an account ?<a class="SignUpLink" href="/signup">SignUp</a>
          </p>
        </div>
      </form>
    </div>
    <div class="info-content Login">
      <h2>WELCOME BACK!</h2>
      <p>Continue managing your account and access all exclusive features now</p>
    </div>

  </div>
  <script src="index.js"></script>
</body>





 <!-- 
    <div class="form-container">
        <h2>Login</h2>
        <form action="/login" method="post">
          <div class="form-group">
            <label for="name">Name:</label>
            <input type="text" id="name" name="username" placeholder="Enter your name" required autocomplete="off">
          </div>
          <div class="form-group">
            <label for="password">password:</label>
            <input type="password" id="password" name="password" placeholder="password" required>
          </div>
          <button type="submit" class="submit-btn">Login</button>
        </form>
        <p>Don't have an account? <a class="link" href="/signup">SignUp</a></p>
      </div> -->

</html>